// name, age, height
class Person {
    constructor(name, age, height) {
        this.name = name;
        this.age = age;
        this.height = height;
    }
}

const teo = new Person('Teo', 18, 150);
const ti = new Pesron('Ti', 10, 180);
const tun = new Person('Tun', 20, 140);

const arrPerson = [teo, ti, tun];
